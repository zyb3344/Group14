name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set working directory
      run: |
        cd Code-Development  # Change this to the root directory of your project

    - name: Setup CMake
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake

    - name: Check if build directory exists
      run: |
        if [ ! -d "build" ]; then
          mkdir build
        fi

    - name: Build project
      run: |
        cd build
        cmake ..
        make

  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set working directory
      run: |
        cd Code-Development

    - name: Setup CMake
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake

    - name: Check if build directory exists
      run: |
        if [ ! -d "build" ]; then
          mkdir build
        fi

    - name: Build project
      run: |
        cd build
        cmake ..
        make

    - name: Run tests
      run: |
        cd build
        ./GSTest
